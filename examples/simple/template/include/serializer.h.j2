// automatically generated by the Flatt tool, do not modify

#pragma once

#include <stdint.h>
#include <vector>

#include "packets.h"
#include "packets_generated.h"

namespace protocol {
namespace serializer {

{%- for packet in packets %}
  inline packet {{ packet.name_snake }}(const {{ cpp_case(packet.namespace) }}::{{ packet.name }}T& packet)
  {
    packet pkt = { {{ packet.name_snake }}_id, packet::buffer(/* TODO: size of packet */) };
    // TODO: copy packet into into pkt.content.data()
    return pkt;
  }

{%- endfor %}
} // namespace serializer
} // namespace protocol
