// automatically generated by the Flatt tool, do not modify

#pragma once

#include <stdint.h>

#include "packets.h"
#include "packets_generated.h"

namespace protocol
{
  template<typename Handler>
  inline void parse(const packet& pkt, Handler&& handler)
  {
    switch (pkt.id)
    {
      case protocol::packets::auth_request:
        {
          auto parsed = /* TODO: transform `packet.content` into `AuthRequest` */;
          handler<AuthRequest>(parsed);
        }
        break;

      case protocol::packets::auth_response:
        {
          auto parsed = /* TODO: transform `packet.content` into `AuthResponse` */;
          handler<AuthResponse>(parsed);
        }
        break;

    }
  }
} // namespace protocol
