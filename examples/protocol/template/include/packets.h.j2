// automatically generated by the Flatt tool, do not modify

#pragma once

#include <stdint.h>
#include <vector>

namespace protocol
{
  enum class packets : uint32_t
  {
{%- for packet in packets %}
    {{ packet.name_snake }} = {{ hex(packet.id) }},
{%- endfor %}
  };

  struct packet
  {
    using buffer = std::vector<uint8_t>;

    packets id;
    buffer content;
  };

} // namespace protocol
